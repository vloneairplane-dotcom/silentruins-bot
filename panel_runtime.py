"""Compatibility shim برای Railway.

نقطه‌ی شروع اصلی خودِ bot.py است (همه‌ی لایه‌های هوش داخلش فعال می‌شن)؛
این فایل فقط برای سازگاری با startCommand قبلی نگه داشته شده.
"""

import bot

if __name__ == "__main__":
    bot.main()
